PORT=4000
NODE_ENV=development
JWT_SECRET=revaudit_dev_secret_key_2026
JWT_EXPIRES_IN=7d

# Database Connection (Optional Postgres, defaults to local SQLite if omitted)
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/revaudit

# Storage Configuration
STORAGE_DRIVER=local

# Statistical Engine Microservice Target
PYTHON_ENGINE_URL=http://127.0.0.1:8000
