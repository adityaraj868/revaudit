services:
  - type: web
    name: revaudit-backend
    env: python
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
